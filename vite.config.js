import { defineConfig } from 'vite'

export default defineConfig({
  // For GitHub Pages: set base to '/<your-repo-name>/'
  // e.g. if repo is github.com/you/emotion-detector, use '/emotion-detector/'
  base: '/emotion_detector/',
})
